# TM-CBM 원본 데이터 마이그레이션 — 완료 (2026-08-13 기준)

Lovable 프로젝트(tm-cbm, project_id `b386a24f-2d99-4a20-b824-3efe76ecfa76`,
workspace `GS8VAndim2d3OBu4TcTX` = "권휘진's Lovable")에 원본 데이터를 반영하고
기능을 정비한 작업 기록. 계획했던 항목 전부 완료됨.

## 완료된 것
- `tm_assets` 171건, `repair_history` **128건(원본 전체)** DB 삽입 완료.
  111~116편성 각 24대 + 예비품 27건. 위치코드(M01~M24)는
  `CAR_PREFIX=[11,12,14,15,17,18]` + 편성 끝 2자리 공식으로 정확한 실차호(예: `1711`)로 변환됨.
- 예비품 `9134547`: 제작년도/제조사 **1991년/도시바**로 확정.
- **RP-0082 반영 완료** — 113편성 1413-TM3, 취거88TWDH114→부착88091-73, 날짜 **2023-07-03** 확정.
- **src/routes/index.tsx 차호 표시 수정 완료** — `{car}차` → `{car}호차` (커밋 `b5c9b50`).
- **전체 기능 QA 완료** — 챗봇/고장추세배지/대시보드 테스트, 버그 2건 발견·수정
  (편성 오인식, "3호차" 순번 질의 매칭 오류).
- **챗봇 인사말 버그 수정 완료** — "안녕" 등에 자연스러운 인사+안내로 응답.
- **시리얼 `???`(111편성 1511-TM4)** — 사용자 확인: 원본 자체가 미상이라 그대로 둠.
- **외부수선이력 완료** — `external_repair_log` 테이블 신설(47건: 수선반출 35 + 불용 12),
  `/external-repair` 페이지 추가(검색/필터/예비품 연결배지).
- **구글시트 연동 검토 후 취소** — Google Sheets/Drive는 워크스페이스 소유자가 Google
  Cloud Console에서 OAuth 웹 클라이언트를 직접 발급받아야 하는 개발자 작업임을 확인,
  "사용자가 쓰기 힘들다"고 판단해 취소.
- **"엑셀로 내보내기" 버튼 완료** — 취부 현황판 상단에 버튼 추가, 클릭 시
  TM마스터/수선이력/외부수선이력을 각 시트로 나눈 `TM현황_YYYYMMDD.xlsx` 즉시 다운로드
  (로그인 불필요). 브라우저 실제 클릭 테스트로 171/128/47건 정확히 출력됨을 확인.
- 단일 HTML 파일(`vs code ver.html`)에 "고장이력 10/20/전체 보기" 기능 추가 완료.

## 남은 것 (급하지 않음, 필요시 나중에)
- 단일 HTML 파일의 로컬 파일연동 기능(`btnXlsxLink`)을 참고 자료로 남겨둠.
- 사내 폐쇄망 이전 (GitHub export + 백엔드를 사내 PostgreSQL로 교체) — 별도 설계 필요.

## 파일 설명
- `raw_original.json` — 사용자가 제공한 원본 데이터 (master 171건 + repl 128건, 변환 전).
- `convert_fixed.py` — `raw_original.json` → 앱 스키마로 변환하는 스크립트 (CAR_PREFIX 공식 포함).
- `fixed_master.json` / `fixed_repl.json` — 변환된 결과 (DB와 동일, 128건 전체 포함).
- `repair_history_migration.sql` — 실제 DB에 삽입 완료된 SQL (RP-0082 제외 127건분). 재실행 불필요.
