# TM-CBM 작업 기록 — 완료 (2026-08-14 기준)

Lovable 프로젝트(tm-cbm, project_id `b386a24f-2d99-4a20-b824-3efe76ecfa76`,
workspace `GS8VAndim2d3OBu4TcTX` = "권휘진's Lovable")에 원본 데이터를 반영하고
기능을 정비한 작업 기록. **오늘(2026-08-14)까지 지시된 항목 전부 완료 및 배포됨.**

- 운영 주소: https://tm-cbm.lovable.app
- 담당자 비밀번호(2026-08-14 기준): `tm1234`

## 2026-08-13까지 완료된 것
- `tm_assets` 171건, `repair_history` 128건(원본 전체) DB 삽입.
  111~116편성 각 24대 + 예비품 27건. 위치코드(M01~M24)는
  `CAR_PREFIX=[11,12,14,15,17,18]` + 편성 끝 2자리 공식으로 정확한 실차호로 변환됨.
- RP-0082 반영 완료 — 113편성 1413-TM3, 취거88TWDH114→부착88091-73, 날짜 2023-07-03 확정.
- 전체 기능 QA 완료 — 챗봇/고장추세배지/대시보드 테스트, 버그 2건 발견·수정.
- 외부수선이력 완료 — `external_repair_log` 테이블(47건), `/external-repair` 페이지.
- "엑셀로 내보내기" 버튼 완료 — TM마스터/수선이력/외부수선이력 3시트 xlsx 다운로드.
- 단일 HTML 파일(`vs code ver.html`)에 "고장이력 10/20/전체 보기" 기능 추가.

## 2026-08-14 완료된 것

- **고장코드 키워드 보강** — FC03에 "절손", FC05에 "F/O"·"플래시오버"·"전식"·"아크" 추가.
  기타(FC99) 비율 37.5% → 12.5%로 개선.
- **챗봇 대규모 업그레이드** — 인사말/조회 버그 수정, 존재하지 않는 편성 안내, 등급별 집계
  요약 응답, 미이해 시 유사 키워드 재질문·예시 제시, 대화형 등록 슬롯필링("지금 붙어있는 거"
  지시어 포함), 등록 전 확인 요약, 고장분류 확인 유도("이 내용이면 FC05로 분류됩니다, 맞나요?"),
  도메인 이탈 질문 처리("날씨는?" 등 → 명확히 안내 + 예시 질문 제시). 부수적으로 챗봇 위젯
  클릭이 안 되던 SSR hydration 버그, 홈페이지 데이터 로드 버그도 같이 수정됨.
- **QR 코드 링크/텍스트 선택** — TM 리포트 QR을 "🔗 리포트 링크" / "📝 텍스트 요약" 두 방식
  중 선택 가능하게. jsQR로 디코딩 검증까지 완료.
- **정적 도움말 페이지** — `/help`, 네비게이션에 추가, 화면별 8개 섹션 + 목차(TOC).
- **표준 기록지 홈페이지 연계** — `TM고장교체이력_기록지.docx`를 `public/`에 저장,
  수선의뢰 등록·도움말 페이지에 "선택사항" 다운로드 링크 추가.
- **TM 리포트 PDF/DOCX 다운로드** — jspdf+html2canvas로 PDF, docx 라이브러리로 Word 다운로드
  버튼 추가 (HWP는 난이도 문제로 보류).
- **UI 최적화** — 검색 부분일치, 모바일 터치 타겟 확대, 등록폼에서 최근 등록 편성/예비품
  우선정렬.
- **등급 필터 복수선택** — "긴급·주의만 보기" 퀵필터 제거, 등급 칩(긴급/주의/관찰/정상/
  예비품/불용대기)을 각각 독립 토글로 변경해 여러 등급 동시 선택 가능. "전체" 버튼으로 일괄
  초기화.
- **검토(OK/확인필요) 상태 정리** — 기존 128건 전부 "OK"로 일괄 처리(원본 데이터는 이미 확인된
  것이므로). 앞으로 신규 입력되는 데이터만 자동 검토 대상.
- **대시보드 고도화** — 제작년도별 고장현황, 제조사별 신뢰도 비교, 편성별 위험 히트맵,
  위험도 TOP 10 랭킹, 예비품 현황 게이지 5개 위젯 추가 + 위젯 표시/숨김 커스터마이징
  (localStorage 저장). 모든 차트(막대/파이/라인)에 한글 라벨과 수치를 직접 표시하도록 개선
  (범례/툴팁 없이도 바로 읽힘).
- **수선의뢰 등록 인증구조 변경** — 로그인 세션으로 화면 전체를 잠그던 방식에서, 로그인 없이
  누구나 `/repair` 폼과 챗봇 등록 대화에 접근 가능하고, 실제 "저장" 시점에만 비밀번호를
  확인하는 방식(`StaffGate`)으로 전환. 한 번 인증하면 그 세션 동안 재확인 안 함. 챗봇에서는
  비밀번호가 `••••••`로 마스킹됨.
- **관리자 화면 UI 정리** — `data-management`/`raw-data`/`integrity` 3개 페이지는 기존대로
  로그인 세션 필요(의도된 유지)하되, 공통 `AdminPageShell`로 목적 명시·카드형 레이아웃·
  아코디언 정리 적용해 가독성 개선.
- **사내 폐쇄망 이전 계획 문서 작성** — `migration-data/사내폐쇄망_이전_계획.md`.
  4단계(GitHub 코드 반출 → Supabase 자체 호스팅 → 사내 서버 배포 → 데이터 이전) 정리.

## 2026-08-15 완료된 것 (Claude 자체 판단으로 추가 고도화)

- **예방정비 임박 TM 위젯 (예지보전)** — "TM-CBM"(Condition-Based Maintenance)의 핵심 취지에
  맞춰, 과거 이력 기반 반응형 등급 표시에서 나아가 앞으로의 정비 시점을 예측하는 기능 추가.
  운영중 TM 중 MTBF 계산이 가능한(고장성 교체 2회 이상) 것들에 대해 `최근 고장일 + MTBF`로
  예상 다음 정비 시점을 산출하고, 오늘 기준 이미 지났거나 90일 이내로 임박한 TM을 경과/임박
  순으로 리스트업. 시리얼/편성·위치/최근 고장일/MTBF/예상시점/경과·임박 배지 표시, 클릭 시
  TM 리포트로 이동. 위젯 커스터마이징 목록에도 추가됨. 실데이터 2건(경과 상태) 확인.

## 2026-08-17 완료된 것

- **챗봇 예방정비 질의응답 + 대화 맥락 기억** — `getPredictiveMaintenance()`를
  `src/lib/tm.ts`의 공용 함수로 분리해 대시보드·챗봇이 공유. "예방정비 필요한 거 있어?",
  "다음에 뭐부터 정비해야 돼?" 같은 질문에 경과/임박 TM을 시리얼·편성·MTBF·예상시점과 함께
  요약 응답. 없으면 안심 답변. 대화 맥락 기억 추가 — 직전에 언급된 편성/시리얼을 "거기",
  "그거" 같은 지시어로 후속 질문에서 이어받음 (예: "111편성 상태 어때?" → "거기 긴급한 거
  있어?"). 브라우저로 두 시나리오 모두 실제 응답 확인 완료.

- **간단 모드 / 전체 메뉴 토글** — 네비게이션 7개 메뉴가 현장 직원에게 부담스럽다는 피드백에
  따라, 헤더에 "간단히 보기 / 전체 메뉴" 토글 추가. 간단 모드(기본값)는 취부 현황·TM 리포트·
  수선의뢰 등록·도움말만 표시, 전체 메뉴는 대시보드·이력 조회·외부수선이력·산출기준까지 다
  표시. 선택 상태는 localStorage에 저장돼 기기별로 유지됨 (현장 태블릿은 간단모드, 사무실
  PC는 전체메뉴로 각각 기억). 모바일 반응형까지 확인.

## 2026-08-18 완료된 것

- **네비게이션 메뉴 순서 정리** — 사용 빈도/논리적 흐름 기준으로 재배치: 취부 현황 →
  대시보드 → 수선의뢰 등록 → 이력 조회 → TM 리포트 → 외부수선이력 → 산출기준 → 도움말.
  담당자 로그인은 항상 맨 오른쪽. 일반모드/관리자모드 각각에서 이 순서 유지.
- **일반모드 / 관리자모드 토글 이름 변경** — 기존 "간단히 보기/전체 메뉴" 명칭을
  "일반모드/관리자모드"로 변경 (버튼 라벨·aria-label·title 전부 일관되게).
- **챗봇 날씨-고장 상관관계 질문 지원** — 기존엔 "날씨"라는 단어만 있으면 무조건 도메인이탈로
  거절했으나, "오늘 날씨 어때?"(실시간 정보 요청, 거절 유지) vs "날씨랑 고장 관련있어?"
  (상관관계 분석 질문, 도메인 내)를 구분하도록 수정. 후자는 repair_history의 FC02(절연저항·
  물유입) 이력과 세부내용의 침수/우천/장마 키워드를 집계해 실데이터로 답변
  (검증 시점 기준 습기 관련 고장 11건/전체 111건 중 9.9%로 응답 확인).
- **일반모드/관리자모드 토글 UX 개선** — 기존엔 버튼 하나로 토글하는 방식이라 "지금 이
  모드다"인지 "누르면 이 모드로 바뀐다"인지 헷갈린다는 현장 피드백 발생. "일반모드"/
  "관리자모드" 두 버튼이 나란히 항상 보이고 현재 선택된 쪽만 파란색으로 강조되는
  세그먼트 컨트롤로 교체(`ModeSegment`, `src/components/AppHeader.tsx`). 데스크톱·모바일
  모두 클릭 시 즉시 메뉴 개수/강조 상태 반영, 새로고침 후에도 선택 유지되는 것 확인.
- **챗봇 온도(추위/더위) 상관관계 질문 지원** — "추울때는?", "더울때는?" 같은 질문이
  전혀 이해되지 않던 문제 수정. 기존 WEATHER_RE엔 습기 관련 키워드만 있고 온도 어간이
  없었던 게 원인. 온도 어간(춥/추워/추울, 더워/더울/한파/폭염 등, 형태소 변화 포함)을
  인식하는 TEMP_RE 추가, 여름철 과열·냉각팬 이력과 겨울철 결로·동절기 이력을 계절별로
  집계해 실데이터로 답변. 직전 대화 주제(`BotContext.lastTopic`)를 기억해서 "날씨랑
  고장 관련있어?" 질문 뒤에 오는 "추울때는?" 같은 짧은 후속 질문도 같은 맥락으로 이어서
  답변. "오늘 기온 몇도야?" 같은 실시간 정보 요청은 기존처럼 거절 유지.
- **챗봇 대폭 고도화 (8개 신규 질문 카테고리)** — 현장직원들의 실제 다양한 질문 패턴에
  대응하도록 확장:
  1) 고장코드 직접 조회 — "FC02가 뭐야?" 등 코드 입력 시 라벨·심각도·키워드 예시 안내,
     존재하지 않는 코드는 실제 코드 목록 안내
  2) TM 상세정보에 제조사·제작년도·연식 추가 표시
  3) 제조사별 신뢰도 — "제조사별로 비교해줘" 등에 보유 대수/고장성 교체 건수/대당 고장률
     실데이터 랭킹으로 답변
  4) 제작년도(연식)별 질문 — "제일 오래된 거 뭐야?", "90년대 이전 몇 개야?" 등 연식
     정렬·구간별 분포 답변
  5) 외부수선이력(external_repair_log) 연동 — "외부수선 이력 몇 건이야?" 등 반출/불용
     구분별 집계, 특정 시리얼 조회 지원
  6) 등록자·최근 등록 현황 — repair_history의 registrant 컬럼 기반 "누가 등록했어?",
     "오늘/이번주 등록 몇 건이야?" 답변
  7) 구어체 동의어 인식 — "이상없어?/괜찮아?"를 정상 여부 확인으로, "제일 위험한 거"를
     우선점검 질문으로 매칭
  8) 범위 밖 질문 처리 — "비밀번호 뭐야?" 등 보안 관련 질문은 명확히 거절, "연락처가
     어디야?" 같은 실제로 모르는 질문은 정직하게 모른다고 안내하며 도움말로 유도
  8개 카테고리 전부 브라우저(Playwright)로 실제 질문 입력 후 응답 확인 완료.
  SUGGESTED_QUESTIONS·HELP_TEXT에도 신규 질문 예시 반영.

## 2026-08-19 완료된 것

- **정비 문서 3종 연계 + RAG 기반 챗봇 질의응답** — 사용자가 제공한 서울교통공사 1호선
  견인전동기 정식 문서 3종을 시스템에 반영:
  1. **수선사양서** (39p, 계약/기술규격/수선방법/시험기준) — PDF가 라인별 이미지 렌더링이라
     텍스트 추출 불가, Tesseract OCR로 코퍼스화 (숫자·규격 오류 가능성 있음)
  2. **정비지침서** (14p, 사양·정격/구조/보수 체계도) — 스캔 이미지, 마찬가지로 OCR 기반
  3. **표준작업절차서** (21p, 작업명/방법/공기구/공정사진 표 형식, 2022.12) — 정상 텍스트
     레이어라 추출 품질 양호(신뢰 가능)
  - 원본 PDF는 `public/docs/`에 저장, `reference_documents` 테이블에 메타데이터(문서유형,
    페이지수, 신뢰도 메모) 등록.
  - 새 `/documents`("지침서/절차서") 페이지 — 문서 카드 목록 + PDF 뷰어(페이지 이동 가능)
    + 다운로드. 현장직원에게도 유용한 참고자료라 일반모드/관리자모드 네비 양쪽에 노출.
  - `/external-repair`(외부수선이력) 페이지에 "수선사양서 보기" 배너 추가.
  - **RAG 파이프라인**: 3개 문서 코퍼스를 79개 청크로 분할해 pgvector에 임베딩 시드
    (Lovable AI 게이트웨이 사용). 챗봇이 정비 절차·규격 관련 질문(기존 규칙 기반 분기에
    안 걸리는 경우)을 감지하면 벡터 검색으로 관련 청크를 찾아 LLM에 근거로 제공, 답변과
    함께 출처(문서명·페이지) 칩을 표시하고 클릭 시 해당 페이지로 문서 뷰어가 열림.
  - **OCR 기반 문서(수선사양서/정비지침서) 출처 답변에는 "※ 스캔 문서 OCR 인식 결과입니다.
    수치·규격은 원문 페이지에서 다시 확인하세요." 주의 문구를 자동 표시** — 표준작업절차서는
    텍스트 신뢰도가 높아 이 캡션 없음.
  - 브라우저 검증 완료: 문서 3종 열람, 외부수선이력→수선사양서 연결, "브러시 교체 방법
    알려줘" 질문에 사양서·절차서 출처 + OCR 캡션이 포함된 RAG 답변 확인.

- **정비지침서 파일 접근 완전 차단 + 문서 연계 확장** — 정비지침서는 원본 스캔 양식이
  좋지 않아 "출처 인용까지만, 파일 열람·다운로드는 절대 불가" 방침으로 전환:
  - `/documents` 페이지에서 정비지침서 카드 제거(수선사양서·표준작업절차서 2개만 노출),
    원본 PDF 파일 자체를 `public/docs/`에서 삭제해 URL로도 접근 불가하게 처리.
    `reference_documents.file_accessible` 컬럼 추가, 정비지침서만 `false`.
  - RAG 챗봇 출처 표시 분리: 수선사양서/표준작업절차서는 기존처럼 클릭 가능한 칩(문서명·
    페이지, 클릭 시 뷰어로 이동), 정비지침서는 "정비지침서 N페이지 (사내 문서, 열람 불가)"
    형태의 클릭 불가 텍스트로만 표시. **RAG 검색·학습 대상에서는 제외되지 않고 3개 문서
    전부 계속 근거로 사용됨** — 표시 방식만 문서 유형별로 다름.
  - `RelatedDocs` 컴포넌트 신설, 고장코드(FC00~FC05, FC99)별 관련 절차서/사양서 페이지
    매핑(`CODE_DOCS`)을 만들어 수선의뢰 등록 폼(고장코드 자동분류 결과 옆), TM 리포트,
    취부현황판 상세 패널(`TmDetailPanel`) 3곳에 "관련 수선 절차 보기" 링크 추가.
  - 브라우저 검증: 정비지침서 PDF 파일 삭제 및 앱 전체에 링크 없음 확인, 챗봇이 정비지침서
    근거 답변 시 클릭 불가 텍스트로만 인용하는 것 확인, 사양서/절차서는 여전히 정상 열람.

- **메뉴/대시보드 정돈 + 챗봇창 UX + 구어체 인식 5건 개선**
  1. **관리자모드 상단 메뉴 그룹화** — 순서는 유지하되(현황→업무→기록·문서→안내→관리자)
     기능 클러스터 사이에 시각적 구분선 추가(데스크톱: 세로 구분선, 모바일: "현황/업무/
     기록·문서/안내/관리자" 소제목)로 "막 나열된 느낌" 해소.
  2. **대시보드 위젯 순서 정합** — 위젯 선택 체크리스트(`WIDGET_DEFS`) 순서를 실제 화면
     배치 순서(현황요약→원인분석→조치우선순위→최근기록)와 정확히 일치시킴. "예비품 현황"
     파이차트도 위젯 주제에 맞게 예비품→불용대기→운영중 순으로 변경.
  3. **챗봇창 크기조절 + 최소화** — 좌상단 드래그 핸들로 320×400~90vw×90vh 범위에서
     크기 조절 가능(localStorage 저장), 최소화 버튼으로 대화 유지한 채 헤더바만 남기고
     접었다 펼 수 있음.
  4. **챗봇 구어체 인식 확장** — "정비는 어케해?" 같은 질문이 전혀 답변되지 않던 문제 수정.
     "정비"·"점검"·"보수"·"어케"·"어떡해" 등을 문서질문 판별 키워드에 추가.
  5. **포괄 질문 단계적 안내** — "정비는 어케해?"처럼 대상이 특정 안 된 포괄 질문엔 문서
     RAG 전체를 바로 쏟아내지 않고 부위별 선택지(브러시·정류자/계자/전기자/베어링·조립/
     절연·시험) chip을 먼저 제시. 320자 넘는 긴 RAG 답변은 앞부분만 보여주고 "…더 보기"로
     펼치게 해 채팅창에서 한번에 부담스러운 양이 안 나가게 함.

- **정비 문서 연계를 사이트 곳곳으로 확장 + 챗봇창 리사이즈 UX 개선**
  - `RelatedDocs` 컴포넌트를 확장(`getCodeDocs`/`RelatedDocLink`/`RelatedDocChips` 추가)해서
    지침서/절차서 메뉴를 따로 찾아가지 않아도 고장 관련 화면에서 자연스럽게 조치 문서를
    발견하도록 연계:
    - **이력조회**: 이력 상세 팝업(`DetailModal`)에 "이 고장 관련 수선 절차·기준" 추가.
    - **외부수선이력**: 기존 수선사양서 배너 1개 → 수선사양서+표준작업절차서 2개로 확장.
    - **대시보드**: "고장유형별 발생 건수" 차트 아래 실제 발생한 FC코드별 관련 절차 칩 추가,
      "위험도 TOP 10" 각 항목에 최근 고장코드 기준 "📄 관련 절차" 인라인 링크 추가.
    - 정비지침서는 기존 원칙대로 텍스트 인용만, 파일 링크는 여전히 제공 안 함.
  - **챗봇창 리사이즈 UX 개선** — 좌상단 20px 아이콘 하나로만 크기조절 가능하던 문제
    ("아무곳에서나 접근 가능해야지" 피드백) 수정. 위쪽 테두리 전체(높이 조절)와 왼쪽 테두리
    전체(너비 조절)를 드래그 가능하게 확장, 모서리는 가로+세로 동시 조절.

- **데이터 내보내기 대폭 확장 (엑셀+PDF, 개별+통합)** — 기존 "엑셀로 내보내기" 버튼(3시트
  합본 xlsx만 가능) → "데이터 내보내기" 드롭다운 패널로 전면 개편. 데이터를 4개 카테고리로
  재구성(편성별 현황=운영중만/예비품 현황=예비품·불용대기/수선이력/외부수선이력):
  - **엑셀**: 카테고리별 개별 xlsx 4개 + 4개 시트로 분리된 통합 xlsx 1개(`TM현황_통합_
    YYYYMMDD.xlsx`), 총 5종.
  - **PDF**: `jspdf-autotable` 도입해 표 형식으로 페이지 넘김 시 헤더 자동 반복. 카테고리별
    개별 PDF 4개 + 4개 섹션으로 구성된 통합 PDF 1개, 총 5종. 한글 깨짐 방지를 위해
    NotoSansKR 폰트 서브셋(`public/fonts/`)을 임베드.
  - 총 10종 다운로드 버튼 전부 브라우저 실다운로드로 검증(통합 엑셀 4시트 분리 확인, PDF
    12페이지 4섹션 144/27/128/47건 데이터 확인). 로그인 없이 기존처럼 다운로드 가능 유지.

- **사이트 전체 접근 비밀번호 게이트 + 벤치마킹 반영 3종** — 참고 사이트(tmcbmtest2.pages.dev)
  벤치마킹 조사 후 반영:
  - **사이트 접근 게이트(신규)**: 비밀번호 `1488`로 사이트 전체를 잠금. 담당자(StaffGate) 저장
    권한과는 완전히 별개 — 비밀번호는 Supabase에 bcrypt 해시로 저장, `verify_site_access`
    RPC(SECURITY DEFINER)로 서버측 검증만 하고 클라이언트 JS엔 평문이 전혀 없음.
    `__root.tsx`에서 라우트 트리 전체를 감싸 통과 전엔 어떤 페이지도 안 보임, 통과 후
    localStorage에 기억. 배포 후 실제로 게이트 화면부터 뜨는 것 확인.
  - **위험도 근거 시각화(RiskGauge)** — 참고 사이트의 A/F/T/M 게이지 아이디어를 우리 O×S
    매트릭스 체계에 맞게 반영. TM 상세패널·TM리포트에 발생도 O·심각도 S를 1~10 스케일
    수평 게이지바로 표시하고 매트릭스 교차 결과(등급)를 강조.
  - **정합성 검토 강화** — 행 단위로 구체적 오류/경고 + 처리 권장사항 표시(예: "같은 위치에
    TM 2대 중복 취부 → 현차 확인 후 예비품으로 변경하세요"), 헤더 "정합성 검토" 메뉴에
    오류 건수 빨간 배지 추가.
  - **위험도 파라미터 설정 화면(신규, `/risk-settings`, 관리자 전용)** — 발생도 O 매핑,
    MTBF 가산 기준, 밴드 경계, 심각도 변환표를 조회·수정 가능. 기본값은 기존 하드코딩 값과
    완전히 동일(`risk_settings` 테이블 시드), 저장 전까지 계산 결과 무변경. 저장 시 확인
    다이얼로그("안전 판정 기준 변경") 후 전체 재계산.
  - 브라우저 검증 전부 완료: 게이트(오답 거부/1488 통과), 게이지바 실데이터 렌더링,
    정합성 검토 오류 목록(전체 이력 128건 중 지적사항 59건·오류 49건) + 헤더 빨간 배지(49),
    위험도 설정화면 기본값이 기존 하드코딩 값과 정확히 일치 + 저장 전 홈 화면 등급 무변경
    (전체 171·긴급 2·주의 13·관찰 2·정상 127·예비품 22·불용대기 5) 모두 관리자 세션으로 확인.

- **챗봇 버그 3건 수정 + 데이터 스냅샷(현재시점저장/복원) + 엑셀 가져오기(업로드)** —
  사용자가 캡처로 제보한 챗봇 문제와, 벤치마킹 사이트에서 착안한 데이터 관리 기능을 반영:
  - **"눈오면?" 등 조사 생략 구어체 미인식 수정**: 날씨 관련 정규식이 "눈이오"처럼 조사가
    있어야만 매칭되던 문제 → `눈\s*(이)?\s*(오|와|온|내리)` 형태로 조사 선택적으로 완화.
    "-면" 조건형 어미도 상관관계 질문 트리거에 추가.
  - **"브러쉬홀더 교환 어떻게해?"가 수선의뢰 등록으로 오작동하던 문제 수정**: 등록 의도
    감지 정규식의 "어" 어미가 "어떻게"의 "어"에도 걸려 오탐 — "어떻게/어케/하는법/하는방법/
    방법" 뒤에는 매칭 안 되도록 부정형 전방탐색(negative lookahead) 추가해 절차 질문과
    등록 요청을 구분.
  - **대화 도중 딴 이야기로 넘어가도 계속 이전 단계에 갇혀 응답 못 하던 문제 수정**: 위 오탐
    버그로 등록 흐름이 잘못 시작되면, 이후 어떤 질문을 해도 그 단계의 좁은 파서가 계속
    "이해 못했어요"만 반복하던 구조적 문제. 사용자가 흐름과 무관한 질문/주제 전환을 하면
    자동으로 등록 진행을 취소하고("수선의뢰 등록은 잠시 멈췄어요") 원래 질문에 정상 답변하는
    로직(`isTopicChange`/`looksLikeExpected`) 추가. 단, 이번 챗봇은 규칙(정규식) 기반이며
    지식그래프·LLM 기반 의미 이해는 아님 — 사용자가 요청한 수준의 진짜 문맥 이해는 별도의
    큰 구조 변경이 필요하다는 점을 안내함.
  - **데이터 스냅샷 저장/복원(신규, `/data-management`, 관리자 전용)**: 현재 시점의
    tm_assets/repair_history/external_repair_log 전체를 이름 붙여 저장(`data_snapshots`
    테이블, RLS로 관리자만 접근). 복원 시 확인 다이얼로그 필수 + 복원 직전 자동으로
    "복원 전 자동 백업" 스냅샷을 먼저 생성한 뒤 덮어써 실수로 데이터를 잃지 않도록 안전장치.
    실제 다인원 공유 DB이므로 "예전 시점으로 되돌리기"가 실수 시 되돌릴 수 없는 위험이 없게
    설계.
  - **엑셀 가져오기(신규, 같은 화면)**: 기존 "데이터 내보내기" 양식과 헤더를 그대로 재사용해
    양방향 호환. 파일 업로드 → 미리보기(시트별 신규/수정/오류 건수 + 행별 문제점) →
    "반영" 버튼으로 명시적 확정하는 2단계 구조(업로드 즉시 덮어쓰지 않음). tm_assets는
    시리얼 기준 upsert, repair_history는 신규 건만 삽입. 정합성 검사 로직을 재사용해
    가져오는 데이터도 함께 교차 검증.
  - 브라우저 검증 완료(관리자 세션): 스냅샷 저장→목록에 171/128/47 표시→복원 클릭 시
    확인창→실행 후 "복원 전 자동 백업" 스냅샷 자동 생성 확인. 앱에서 내보낸
    `편성별현황_20260829.xlsx`를 그대로 업로드→미리보기 "신규 0건·수정 144건·오류 0건"→
    반영 후 전동기 171대·이력 128건 그대로 유지(중복 생성 없음), 콘솔 오류 없음. 검증용으로
    만든 스냅샷 2건은 DB에 남아있어 필요시 관리자가 삭제 버튼으로 정리 가능.

- **산출기준 페이지 재구성 + 메뉴 활성 강조 + 챗봇 안내문구 + 모드저장 버그 4건** —
  - **`/risk-criteria`(산출기준) 순서 개편**: 결론(등급 매트릭스 + "등급별로 무엇을 해야
    하나요?")을 맨 위로 올리고, 발생도 O·심각도 S 상세 표는 "…어떻게 계산되나요?"
    아코디언 2개로 접어서 아래로 내림. 필요한 사람만 펼쳐보는 구조로 변경. 브라우저에서
    아코디언 펼침/접힘 및 모바일(390px) 레이아웃 확인.
  - **상단 메뉴 활성 강조**: `activeProps` 대신 TanStack Link의 `data-status="active"`를
    이용해 `data-[status=active]:bg-primary/15` + `text-primary` + `font-semibold` +
    하단 밑줄로 뚜렷하게 강조. 쿼리스트링이 붙는 라우트(`/asset-report?serial=x`)도
    `activeOptions={{ includeSearch: false }}`로 정상 활성 표시 확인.
  - **메뉴모드(일반/관리자) 새로고침 시 리셋되던 버그 수정**: localStorage 저장을 마운트
    시점 effect가 아니라 클릭 핸들러로 옮겨 해결. 관리자모드 선택 → 새로고침 후에도
    유지되는 것 브라우저로 최종 확인.
  - **모바일 메뉴 글씨 크기 불일치 제보 확인**: "취부 현황" 항목만 커 보인다는 제보 →
    브라우저에서 실측한 결과 모바일(390px) 메뉴 항목 전부 14px로 동일, 재현되지 않음
    (스크린샷 상 배지·강조 배경 때문에 시각적으로 커 보였을 가능성).
  - **챗봇 "날씨가 어때?" 답변에 안내문구 추가**: 실제 날씨 정보가 아니라 습기·침수 관련
    고장 이력 통계를 보여주는 기능인데 설명 없이 통계만 나와 헷갈린다는 피드백 → 답변
    맨 앞에 "저는 실시간 날씨 정보는 알 수 없지만, 날씨(습기·침수 등)와 관련된 고장
    이력을 보여드릴게요."를 추가하고 하단 중복 안내문구는 정리.
  - 전부 배포 완료(commit `81cca05`), 사이트 접속 시 게이트 화면 정상 확인.

## 보안 스캔 항목

- **critical 2건 — 해결 완료, 배포됨(commit `b5c3b76b`)**:
  - **감사 로그(tm_asset_audit) 이메일 노출**: SELECT 권한을 `authenticated`로만
    제한하고 `anon` SELECT 권한 회수. 비로그인 REST 조회 시 42501(거부) 반환 확인.
  - **인증된 사용자의 자산/이력 무제한 수정**: `public.user_roles` + `has_role()`
    함수 신규 추가, `tm_assets`/`repair_history`/`external_repair_log`의
    INSERT/UPDATE/DELETE 정책을 전부 "admin 역할이 있는 계정만" 가능하도록 변경
    (기존 계정엔 admin 역할 부여). 브라우저 검증: 담당자 계정 수정 성공, 위조 시도
    (다른 사람 이름으로 감사기록 작성)는 차단, 비로그인 열람 차단 확인.
  - 이어서 `risk_settings`(위험도 기준)와 `data_snapshots`(백업 스냅샷)의 쓰기 권한도
    동일하게 admin 전용으로 강화(commit `0d45a5a2`). 담당자 계정으로 빠른저장/삭제/
    조회 정상 동작 확인.
- **경고 2건 — 사용자 확인 완료**:
  - **현황판/수선이력 로그인 없이 조회 가능**: 앱이 로그인 없이 현황판을 보도록 설계돼
    있어 의도된 동작으로 그대로 둠.
  - **유출 비밀번호 차단(Leaked Password Protection) 기능 꺼짐**: 사용자가 "켠다"로
    확정 → 활성화 요청 발송함.

- **대시보드 위젯 드래그 이동/크기조절 + 챗봇 개념답변/RAG폴백 + 헤더 빠른저장 + 메뉴 글씨
  굵기 완화 — 4건 전부 완료 및 배포 완료(commit `c8ad4fa8`)**:
  - **대시보드 위젯 자유배치**: `react-grid-layout` 기반 `ResponsiveGridLayout`으로
    드래그 이동·리사이즈 가능. 레이아웃(x/y/w/h)은 localStorage에 저장돼 새로고침해도
    유지되고 "기본 배치로 초기화" 버튼으로 복구 가능. 640px 미만에서는 자동으로 1열
    고정+드래그 비활성화. 브라우저에서 드래그(위치 이동) → 새로고침 후 유지 → 초기화
    버튼 → 모바일 1열 고정까지 전부 확인.
  - **챗봇 "견인전동기가 뭐야?" 등 개념 질문 답변**: `conceptAnswer`로 정의·역할·구조
    개요를 답하도록 추가. "견인전동기 상세설명" 클릭 시 실제 설명이 나오는 것 확인.
  - **챗봇 RAG 폴백 보강**: 출처를 못 찾거나 "찾지 못했" 응답일 때 `DOC_CANDIDATE_TEXT`
    안내문 + `docCandidates()` 관련 문서 후보 칩을 제시하도록 변경. "우주선 엔진 교체
    절차" 같은 완전히 무관한 질문에도 후보 칩 3종이 뜨는 것 확인.
  - **헤더 "빠른 저장" 버튼**: 관리자 로그인 상태에서 헤더(데스크톱/모바일)에 노출,
    클릭 시 이름 입력 없이 "빠른저장 YYYY-MM-DD HH:mm"로 즉시 스냅샷 저장 + 토스트
    안내. 데이터관리 화면까지 안 가도 바로 백업 가능해짐. 클릭 → 저장 → 토스트 확인.
  - **모바일 메뉴 활성 항목 글씨 굵기**: `font-semibold` → `font-medium`으로 완화,
    배경색/좌측 강조선 위주로 구분되도록 톤 다운. 전 항목 14px 동일 재확인.

- **정비지침서(TM_maintenance_manual.docx) 문서 라이브러리 추가 — 완료, 배포됨**:
  사용자가 제공한 서울교통공사 견인전동기 정비지침서 발췌본(113페이지, 사양·구조·
  정기검사기준·브러시 치수/압력 등 포함, 대외비 표시 없음)을 PDF로 변환해
  `public/docs/tm-maintenance-manual.pdf`로 저장, `reference_documents` 테이블에
  등록, RAG용으로 106개 청크 인덱싱. 브라우저 검증 완료: 문서 목록에 "견인전동기
  정비지침서 발췌본(지침서, 113p)" 표시, PDF 뷰어 정상 로드, 챗봇에 "브러시 치수가
  뭐야?" 질문 시 "64 x 40 x 20"을 정비지침서 76·50·48·52페이지 출처와 함께 답변하는 것
  확인. 배포 완료(commit `43c9fe0`).

- **"빠른 저장" 접근 방식 수정 + 취부 현황판 TM 위치 맞바꾸기 기능 — 완료, 배포됨**:
  - **빠른 저장**: `AppHeader.tsx`의 "빠른 저장" 버튼을 `isAdmin` 조건 밖으로 빼서
    로그인 여부와 무관하게 데스크톱·모바일 헤더에 항상 노출. 클릭 시 `useStaffGate()`
    (저장 직전 담당자 비밀번호 확인, 챗봇 등록과 동일 UX)로 확인 후 "빠른저장
    YYYY-MM-DD HH:mm"로 즉시 스냅샷 저장. 브라우저 검증: 비로그인 상태에서 오답 시
    "비밀번호가 틀렸습니다." 오류, 정답 시 정상 저장+토스트 확인.
  - **TM 위치 맞바꾸기(정정)**: 신규 `SwapLocationDialog.tsx` — 취부현황판에서 TM
    선택 → 상세패널 "데이터 정정 > 다른 TM과 위치 교환" → 대상 검색·선택 → 확인
    다이얼로그+담당자 비밀번호 → `tm_assets` 편성/위치 필드 맞교환. 새 테이블 없이
    기존 `tm_asset_audit`에 `action: 'swap'`으로 두 시리얼 각각 1행씩 기록
    (`repair_history`는 미기록 — 고장 교체 아님). 브라우저 검증: `296640-10` ↔
    `98TWDH397` 실제 교환 확인, `tm_asset_audit`에 감사 기록 2건 생성 확인.
  - 배포 완료(commit `1193d956`).

- **모바일/태블릿/데스크톱 반응형 전체 점검 — 완료, 배포됨**: 375/390/768/1280/1920px
  전 화면(취부현황판, 대시보드, 수선의뢰, 이력조회, 외부수선이력, TM리포트, 문서,
  산출기준, 도움말 등) 자동 점검. 결과: 가로 스크롤 전 화면·전 폭 없음, 표는 전부
  가로 스크롤 컨테이너로 정상 동작, 모달(비밀번호/게이트/이력상세/위치교환/스냅샷
  복원)도 작은 화면 안에 정상적으로 들어감. 발견·수정한 문제:
  - 태블릿(768px) 대시보드 위젯이 12칸 기준 그대로 눌려 좁아 보이던 문제 → 태블릿
    구간(640~1023px)은 6칸 기준+행높이 확대로 수정, 이 폭의 배치 자동압축 결과가
    데스크톱 배치를 덮어쓰지 않도록 저장 차단.
  - 모바일 터치 타겟이 너무 작던 요소들(모드 전환 버튼 29px, 위젯 체크박스 14px,
    "표준 기록지 다운로드"·"현재 예비품 보관 중" 링크 20~21px, 관련 문서 칩 27px)을
    전부 최소 32px 이상으로 확대, 체크박스도 14→16px.
  - 스냅샷 복원 확인창에 높이 제한(85vh)+내부 스크롤 적용해 작은 화면에서 화면 밖으로
    안 나가게 수정.
  - 브라우저 재검증(375/390/768px): 가로 스크롤 없음, 375px에서 대시보드 위젯 전체
    정상 표시(스크롤 후 확인) 재확인. 배포 완료(commit `a7db617c`).

- **챗봇 RAG 답변 품질 개선 — 완료, 배포됨**: 조사 결과 `askDocuments`
  (`src/lib/rag.functions.ts`)는 이미 임베딩(512차원) 벡터 검색 + LLM 요약을 쓰고
  있었음(단순 키워드 매칭 아님). 다만 프롬프트가 원문 인용 위주였고 검색 임계값도
  느슨했던 문제를 개선:
  - **프롬프트 재설계**(`src/lib/rag.server.ts`): "결론 → 핵심 수치/한도 → 작업 순서
    → 주의사항" 실무형 구성으로 재지시, OCR 오탈자는 정리해 자연스러운 문장으로
    재구성하도록 변경(해당 정보 없는 항목은 생략). 출처 표기([1][2] 발췌 번호)는 유지.
  - **검색 정확도 보강**(`src/lib/rag.functions.ts`): 상위 12개 후보로 넓힌 뒤 절대
    임계값 0.22 + 최고점 대비 상대 임계값 0.75로 관련성 낮은 조각을 걸러 최대 5개만
    사용(기존: 상위 6개 고정+임계값 0.2 느슨).
  - 브라우저 검증: "브러시 치수가 뭐야?" → 결론/수치/순서 구성으로 답변(64×40×20mm),
    출처 표시 유지 확인. "정기검사 기준" 등 문서에 명확한 근거가 없는 질문은 임계값이
    걸러 "찾지 못했습니다"로 정직하게 답하는 것도 확인(과도한 추측 방지가 의도대로 동작).
    배포 완료(commit `913fe393`).

- **취부 현황판 예비품/불용대기 정렬·그룹핑 개선 — 완료, 배포됨**: 기존엔 `status ===
  "예비품"`인 자산을 필터만 하고 정렬 없이(DB 조회 순서 그대로) 렌더링하던 것을,
  예비품 그룹(22대)과 불용대기 그룹(5대, `note`에 "불용" 포함 여부로 판정)으로 분리,
  각 그룹 안에서는 시리얼 오름차순 정렬, 그룹별 제목에 개수 표시하도록 개선
  (`src/routes/index.tsx`). 브라우저 검증: 예비품 22대 → 불용대기 5대 순으로 명확히
  구분되고 각각 시리얼순 정렬 확인. 배포 완료(commit `2e151d04`).

- **사이트 입장 비밀번호(1488)와 데이터수정 확인 비밀번호(9717) 분리 — 완료, 배포됨**:
  SiteGate(사이트 게이트)와 useStaffGate(데이터 수정 확인/직원 로그인)를 완전히
  독립된 값으로 분리. 사이트 게이트는 1488 유지, 데이터 수정 확인·빠른저장·TM
  위치교환·챗봇 수선의뢰등록·직원 로그인에 쓰이는 비밀번호는 9717로 변경. 브라우저
  교차검증: 사이트 입장 화면은 1488만 통과(9717 거부), 데이터 수정 확인창과 직원
  로그인은 9717만 통과(1488 거부) 확인. ※ 9717은 4자리 숫자라 보안 강도가 약함을
  사용자에게 안내했으나 사용자가 "이대로 진행"으로 확정하여 반영함. 배포 완료.

- **모바일/데스크톱 메뉴 디자인 개선 — 완료, 배포됨(commit `9b9389ca`)**: 섹션 구분
  라벨을 굵은 대문자+넓은 자간+오른쪽 구분선으로 명확한 디자인 요소로 변경, 활성
  항목은 옅은 배경에서 진한 솔리드 강조(모바일·데스크톱 톤 통일)로 강화, 아이콘 18px
  통일, 항목/그룹 간 여백 정리. 타입체크 통과, 모바일(390px)/데스크톱(1280px)
  스크린샷으로 활성 상태 강조 확인.

- **챗봇 "전동기 작동원리" 등 명사형 개념 질문 인식 — 완료, 배포됨(commit `4ca95645`)**:
  `CONCEPT_ASK_RE`/`isPrincipleQuestion`에 "구조/개념/생김새/어떻게 돼" 등 추가해
  질문형 어미 없는 명사형 질문도 인식하도록 확장. `conceptAnswer()`에 실제 작동원리
  설명 보강(계자 전류→자기장 형성→전기자 전류와 상호작용해 로렌츠 힘→회전 토크→
  정류자·브러시가 전류 방향 전환→연속 회전). "전동기 작동원리"/"TM 구조가 어떻게
  돼"/"견인전동기 원리 설명해줘" 3가지 표현 모두 브라우저에서 정상 응답 확인. 덤으로
  RAG 검색 실패 시 사용자 질문이 중복 표시되던 버그도 같이 수정.

- **모바일 핀치줌(확대/축소) 허용 — 완료, 배포됨(commit `904bb937`)**: viewport meta에
  `maximum-scale=5, user-scalable=yes` 명시적으로 추가, PWA manifest의 display를
  `standalone`→`minimal-ui`로 변경(홈화면 추가 후에도 핀치줌 가능하도록). 모바일
  390px 레이아웃 정상, 핀치줌 동작(scale 1.0→1.2) 확인.

- **데이터 스냅샷 "보기(읽기 전용 조회)" 기능 — 완료, 배포됨**: `SnapshotViewer.tsx`
  신규 작성, 스냅샷 목록 각 행에 "보기" 버튼 연결, DB 쓰기 없이 payload(JSON)만으로
  건수·등급별·편성별 요약과 탭별 표(최대 300행) 렌더링, "실제 데이터 아님" 안내 배너
  포함. 브라우저 검증(저장→보기→내용 확인) 및 타입체크 통과, 배포 완료.

- **TM 리포트 HWP 다운로드 — 완료, 배포됨**: `/asset-report` 화면에 PDF/DOCX 버튼
  옆 "HWP 다운로드" 버튼 실제 클릭→다운로드 검증. 받은 파일(`TM리포트_296631-4_
  20260915.hwpx`) 구조 검사: zip 유효, mimetype이 첫 엔트리+무압축(STORE), 
  `Contents/section0.xml`이 well-formed XML, 추출한 텍스트가 화면에 표시된 실제
  데이터(편성/위치/등급/발생도/심각도/이력)와 정확히 일치 확인. 타입체크 통과.

- **유출 비밀번호 차단(Leaked Password Protection) 활성화 — 완료**: Supabase Auth
  `password_hibp_enabled: true`로 설정. 기존 로그인(9717)에는 영향 없음(신규 비밀번호
  설정 시에만 검사)을 브라우저로 확인. 향후 비밀번호를 바꿀 때 9717 같은 유출된 값은
  거부될 수 있음 — 그때는 문자+숫자 조합 10자 이상 권장.

- **챗봇 RAG 세계적 수준 고도화 — 완료, 배포됨(commit `ccbf936c`)**: 질의 재구성
  (Query Rewriting)으로 검색어·핵심 낱말 추출 → 의미검색(vector)+단어검색(lexical,
  신규 RPC `search_document_chunks_lexical`) 결과를 합쳐 순위 매기는 하이브리드 검색
  도입, 재랭킹으로 무관한 조각 제거. 근거 없으면 절대 지어내지 않고 "찾지 못했습니다"로
  답변, 모든 수치에 출처 번호 표시. 브라우저 검증: "브러시 규격?" → 규격표만 정확히
  (GHI-1351/20×40×64㎜), "정기검사 주기+교체기준" 복합질문 → 항목별 분리 답변(없는
  항목은 "문서에서 확인되지 않음"), 문서에 없는 도색 색상 질문 → 지어내지 않고 관련
  문서만 안내. 응답 시간 5~20초. 타입체크 통과.

- **대시보드 위젯 드래그 이동 안 되던 버그 — 완료, 배포됨(commit `499c80ff`)**: 원인은
  태블릿·작은 노트북 폭(640~1024px)에서 `react-grid-layout`의 드래그·리사이즈가 아예
  꺼져 있었던 것. 모든 화면 폭에서 동작하도록 수정 — 데스크톱/태블릿은 제목 드래그로
  위치 이동+모서리 드래그로 크기 조절(마우스·터치 둘 다 확인), 모바일(640px 미만)은
  위젯 우측 상단 ▲▼ 버튼으로 순서 변경. 화면 크기별로 배치를 따로 저장해 새로고침해도
  유지됨을 확인. 타입체크 통과.

- **TM 리포트 HWP 다운로드 — 완료, 배포됨**: `/asset-report` 화면에 PDF/DOCX 버튼
  옆 "HWP 다운로드" 버튼 실제 클릭→다운로드 검증. 받은 파일(`TM리포트_296631-4_
  20260915.hwpx`) 구조 검사: zip 유효, mimetype이 첫 엔트리+무압축(STORE), 
  `Contents/section0.xml`이 well-formed XML, 추출한 텍스트가 화면에 표시된 실제
  데이터(편성/위치/등급/발생도/심각도/이력)와 정확히 일치 확인. 타입체크 통과.

- **기존 정합성 오류 59건 "확인됨" 처리 — 완료, 배포됨**: 이미 사람이 검토 완료한
  기존 지적사항을 "확인됨/예외처리"로 일괄 처리해 빨간 배지에서 제외. 새로 들어오는
  이력 데이터의 정합성 문제는 계속 정상적으로 검출되는 것까지 확인.

- **일반모드/관리자모드 구조 개편 — 완료, 배포됨(commit `9e0de9d1`)**: 모드 토글을
  완전히 제거하고 대시보드(위젯 포함)·이력조회·외부수선이력·산출기준·정합성검토·
  위험도설정·원본데이터·데이터관리를 전부 로그인 없이 공개 조회로 전환, 메뉴도 항상
  전체(현황/업무/기록·문서/관리/안내) 노출. 실제 저장·수정·삭제 행동(위험도 저장,
  원본데이터 수정/삭제, 스냅샷 저장·복원·삭제, 엑셀 반영)에서만 기존 useStaffGate
  (9717)로 확인하도록 변경 — 스냅샷 목록도 "담당자 확인" 버튼을 거쳐야 표시됨. 위젯
  드래그·크기조절은 개인 브라우저 설정이라 비밀번호 없이 그대로 동작. 브라우저 검증:
  로그인 없이 모든 화면 열림, 저장 시도 시 비밀번호 창 뜸, 타입체크 통과.

- **정비지침서 공개 열람 전환 — 완료, 배포됨(commit `588710d9`)**: 처음엔 스캔 품질이
  나쁜 버전(14p) 때문에 "사내 문서, 파일 열람 불가"로 막아뒀었는데, 사용자가 직접
  올린 정상 버전(TM_maintenance_manual, 113p 발췌본, 대외비 표시 없음)은 수선사양서·
  표준작업절차서처럼 완전히 공개 열람 가능하게 전환. `/documents` 화면 PDF 뷰어 정상
  로드, `/external-repair`에 "정비지침서 보기" 배너 추가, 챗봇 출처 인용도 클릭 가능한
  칩으로 변경(클릭 시 해당 페이지로 문서 뷰어 이동) 확인. 옛 14p 스캔본은 원본 파일이
  없어 텍스트 인용만 유지하되 안내 문구를 "열람 불가"에서 "원문 파일 없음"으로 정정.

- **"장비유형 확장 설정" 신규 화면 추가 — 완료, 배포됨(commit `56e21ba2`)**: 공모/실적
  자료에 "다른 설비로 횡단전개 가능한 구조"를 보여줄 근거 화면. 완전히 별도의 새 표
  (`equipment_type_configs`)만 사용하며 기존 전동기·이력·대시보드·위험도계산·
  취부현황판은 전혀 건드리지 않음(브라우저로 기존 화면 무변화 확인). 데모용 "제동장치"
  예시 1건 미리 등록, 장비유형명·입력항목·위험도기준·문서카테고리 등록/수정/삭제는
  비밀번호(9717) 확인 후 가능, "초기화" 버튼으로 언제든 데모 기본 상태로 되돌릴 수 있음
  (기존 TM 데이터에는 영향 없음). 타입체크 통과.

- **원본 데이터 수정/수선이력 등록에 되돌리기 기능 — 완료, 배포됨(commit `6bc8187a`)**:
  원본 데이터 수정 시 감사기록(`tm_asset_audit`) 목록에 "되돌리기" 버튼 추가, 클릭+
  비밀번호(9717) 확인 시 old_value로 정확히 복원되고 되돌리기 자체도 새 감사기록으로
  남는 것 확인. 수선이력 상세에 "수정·삭제" 추가(사유 필수 입력+비밀번호 확인). 덤으로
  발견된 버그도 수정: 감사기록 저장 시 로그인 세션 정보가 비동기로 늦게 반영돼 403
  오류가 나던 문제(`currentStaff()` 유틸리티로 해결).

- **헤더 버튼 깨짐 버그 + 전체 UI/UX 디자인 개선 — 완료, 배포됨(commit `455dd4d6`)**:
  1024~1280px 구간에서 메뉴 항목이 너무 많아 헤더 버튼 글자가 세로로 쪼개지던 버그
  수정(2xl 이하에서 "관리" 그룹을 드롭다운으로 축약) — 375/390/768/1024/1280px 전부
  깨짐 0건 확인. 전체 색감을 Linear/Stripe 계열 딥 슬레이트 톤(oklch)으로 교체, 제목
  크기·줄간격·카드 모서리·그림자 통일. 기능·화면구조는 유지, 순수 시각 디자인만 변경.

- **원본 데이터 수정/수선이력 등록에 되돌리기 기능 — 완료, 배포됨(commit `6bc8187a`)**:
  원본 데이터 수정 시 감사기록(`tm_asset_audit`) 목록에 "되돌리기" 버튼 추가, 클릭+
  비밀번호(9717) 확인 시 old_value로 정확히 복원되고 되돌리기 자체도 새 감사기록으로
  남는 것 확인. 수선이력 상세에 "수정·삭제" 추가(사유 필수 입력+비밀번호 확인). 덤으로
  발견된 버그도 수정: 감사기록 저장 시 로그인 세션 정보가 비동기로 늦게 반영돼 403
  오류가 나던 문제(`currentStaff()` 유틸리티로 해결).

- **헤더 버튼 깨짐 버그 + 전체 UI/UX 디자인 개선 — 완료, 배포됨(commit `455dd4d6`)**:
  1024~1280px 구간에서 메뉴 항목이 너무 많아 헤더 버튼 글자가 세로로 쪼개지던 버그
  수정(2xl 이하에서 "관리" 그룹을 드롭다운으로 축약) — 375/390/768/1024/1280px 전부
  깨짐 0건 확인. 전체 색감을 Linear/Stripe 계열 딥 슬레이트 톤(oklch)으로 교체, 제목
  크기·줄간격·카드 모서리·그림자 통일. 기능·화면구조는 유지, 순수 시각 디자인만 변경.

- **내비게이션을 상단 가로 메뉴 → 왼쪽 사이드바 구조로 전환 — 완료, 배포됨
  (commit `8a69ccb8`)**: 메뉴 항목이 15개라 가로 배치가 산만해 보인다는 지적에 따라
  Notion/Linear/Stripe Dashboard 벤치마킹 결과대로 전환. `AppShell.tsx` 신규 작성해
  전체 라우트에 공통 사이드바+최소 상단바 적용(기존 `AppHeader.tsx`는 제거, 각 라우트의
  중복 헤더 호출도 정리). 그룹별(현황/업무/기록·문서/관리/안내) 펼치기/접기, "관리"는
  기본 접힘, 모바일은 슬라이드 드로어, 리포트 인쇄 시 `print:hidden`으로 내비게이션
  숨김 처리. 긴급/정합성 배지 유지 확인. 데스크톱(1280)/태블릿(768)/모바일(390) 검증,
  타입체크 통과.

- **사이드바 열기/접기 아이콘 가시성 개선 — 완료, 배포됨(commit `4caec4c7`)**: 사이드바
  전환 직후 사용자가 스크린샷으로 제보(토글 아이콘이 너무 작고 배경과 안 구분됨).
  40px 테두리+배경 있는 버튼으로 교체, 호버 시 배경색 변화, "메뉴 열기/닫기" 툴팁 추가,
  로고와 세로 구분선으로 시각적 분리. 데스크톱(1280px)/모바일(390px)에서 버튼 표시·
  툴팁·접기펼치기·모바일 드로어 자동닫힘까지 확인, 타입체크 통과.

## 2026-09-21 완료된 것

- **챗봇 AI를 Lovable 게이트웨이(OpenAI)에서 Google Gemini 무료 API로 전환 —
  완료, 배포됨(commit `d7a8b9ff70a4c6a74d92a4b18a4d21491f9bc525`)**: AI 게이트웨이
  유료 크레딧이 반복적으로 소진되던 근본 문제 해결. 사용자가 Google AI Studio에서
  무료 Gemini API 키를 직접 발급(결제 미연결, 무료 등급) → **키 값은 README/git에
  절대 기록하지 않음**, Lovable 프로젝트 백엔드 시크릿(`GEMINI_API_KEY`)으로만
  안전하게 저장.
  - `src/lib/rag.server.ts`의 `embed()`(임베딩)·`callLlm()`(답변생성)을 Lovable
    게이트웨이 대신 Gemini API 직접 호출로 교체: 임베딩은 `gemini-embedding-001`
    (출력 차원을 기존과 동일한 512로 맞춰 `document_chunks` 벡터 컬럼 스키마 변경
    없이 재사용), 답변생성은 `gemini-3.6-flash`. 기존 답변 형식·인용 규칙·
    "찾지 못함" 처리·`AiUnavailableError` 핸들링(이번엔 Gemini 429/403/401 응답
    기준으로 문구 조정)은 그대로 유지, JSON 스키마 강제는 Gemini의 구조화 출력
    (responseSchema)으로 동등 구현.
  - 문서 코퍼스 4종 185개 구간 전체 재임베딩 완료(무료 등급 분당 처리 한도에 맞춰
    배치 간격·재시도 로직 추가, 1차 시도 때 일부 구간 누락돼 재확인 후 재작업).
  - 브라우저 실제 검증: "브러시 규격?", "정류자 삭정 기준이 뭐야?" 질문에 정상
    답변 + 정확한 출처 인용 확인. 타입체크 통과, 배포 완료.
  - **효과**: 챗봇 AI 사용료가 무료 등급 한도 내에서는 사실상 0원. 다만 무료
    티어 자체 레이트리밋(분당 요청 수 제한)은 있어 완전 무제한은 아님 — 사용량이
    아주 많아지면 다시 지연/거부가 발생할 수 있음.

## 남은 것

- **신규 버그·기능 요청 5건 — 요청 문구 준비됨, 빌드 크레딧 소진으로 전송 미완료
  (다음 크레딧 충전 시 최우선 전송 예정)**:
  1. **모바일 핀치줌 축소 불가 버그**: 확대는 되는데 축소가 안 돼서 한 번 확대하면
     화면이 계속 확대 상태로 갇힘. viewport 설정 또는 핀치줌 로직의 최소 배율 제한
     확인 필요.
  2. **대시보드 위젯 "전체 표시" 해제 기능 없음**: 위젯을 전체화면으로 확장하는
     기능은 있는데 원래 크기로 되돌리는 기능이 없음 — 토글 또는 축소 버튼 추가 필요.
  3. **취부현황판 편성 순서 사용자 정렬(신규 기능)**: 편성별 카드/행 표시 순서를
     마우스 드래그로 사용자가 바꿀 수 있게 하고, 브라우저 로컬 저장(개인 설정)으로
     유지.
  4. **"최근 조회 내역" 개별 삭제(신규 기능)**: 현재 항목별 삭제가 안 되는 것으로
     보임 — 각 항목에 삭제(x) 버튼 추가, 기존 전체 삭제 기능은 유지.
  5. **챗봇 답변범위 유도 UX(신규 기능, AI 크레딧 절약 목적)**: RAG 특성상 답변
     가능한 범위(수선사양서·정비지침서·표준작업절차서)가 정해져 있는데, 범위 밖
     질문으로 AI 크레딧만 낭비하고 "찾지 못함"으로 끝나는 경우를 줄이기 위해
     (a) 챗봇 첫 화면에 문서 카테고리 안내, (b) 추천 질문 칩을 실제 문서에 답이
     있는 것들로 보강, (c) 입력창 근처에 범위 안내 문구 추가. TM 자산 데이터 조회
     같은 비-RAG 로직은 그대로 유지.
- **전체 구조 정합성 점검 — 점검 완료, 발견된 사소한 문구 수정은 크레딧 소진으로
  미착수**: "기능을 따로따로 때려박은 느낌이 나면 안 된다"는 우려에 따라 Claude가
  자체 제안했던 점검. 코드 확인 결과 사이드바 메뉴↔실제 화면 일치, useStaffGate
  (비밀번호 확인)가 단일 공통 컴포넌트라 전 화면에서 문구·동작 구조적으로 동일,
  "장비유형 확장 설정"도 같은 아이콘·게이트·카드 레이아웃을 써서 자연스럽게 녹아
  있는 등 전반적으로는 양호. 다만 사소한 문구 불일치 3건 발견:
  1. 비밀번호 확인 버튼 문구가 화면마다 "확인 후 저장" / "담당자 확인 후 저장"으로
     혼재 — 저장이 아닌 삭제·초기화·되돌리기 작업에도 "저장"이라는 단어가 고정 사용됨
  2. 확인창 안내 문구("저장하려면 담당자 비밀번호가 필요합니다")도 실제 동작(삭제 등)과
     무관하게 항상 "저장하려면"으로 고정
  3. 스냅샷 관련 저장 버튼 표현이 "빠른 저장" / "현재시점저장 (스냅샷)" / "스냅샷 저장"
     으로 화면마다 다름
  - 위 3건 모두 순수 텍스트 수정이라 구조 변경 없이 안전하게 고칠 수 있음(Lovable
    추정 10분 내) — 수정 지시를 보내려는 순간 크레딧이 소진되어 **아직 미착수**.
    다음 크레딧 확인 시 진행 예정.
- **사내 폐쇄망 이전 — 완전 차단망으로 확인됨, 자체 호스팅 계획으로 진행**: 처음엔 화면
  공유로 사용자의 업무 PC가 일반 인터넷(구글)과 사내 인트라넷이 둘 다 되는 걸로 보여서
  "도메인 2개만 화이트리스트 등록하면 될 것"이라는 가설을 세웠으나, **정보보안처
  백지윤 과장님이 직접 "내부망은 외부랑 통신 자체가 안 된다"고 확인** — 그 PC는 인터넷망
  PC였고, 실제 업무망(내부망)은 완전 차단망이 맞음. 화이트리스트 가설은 폐기하고, 원래
  계획(`사내폐쇄망_이전_계획.md`, 4단계: GitHub 코드반출→사내 서버에 Supabase 자체
  설치→사내 서버 배포→데이터 이전)으로 진행하기로 함. 참고로 동료가 "Firebase+Codex로
  사내망에 배포했다"던 사례는 확인 결과 사외망(인터넷) 배포였음이 밝혀져 관련 없음—
  오히려 Supabase(오픈소스, 자체 호스팅 가능)가 Firebase(자체 호스팅 공식 미지원)보다
  이 목표에는 더 유리한 선택이었음을 재확인.
  - **다음 단계**: (1) 사내 IT팀에 리눅스 서버·Docker 설치 가능 여부 확인, (2) 정보보안처에
    파일 반입(망간전송) 절차 문의 — 반입 대상은 빌드된 앱 코드, Docker 이미지, DB
    데이터, (선택된 방식에 따라) AI 모델 파일.
  - **미결정 사항 — 챗봇 AI 기능 처리 방식**: 완전 폐쇄망에서는 지금처럼 인터넷 너머
    AI 서버를 호출하는 구조가 작동 안 함. A안(사내 서버에 오픈소스 LLM 직접 설치, 품질
    유지되나 서버 사양·모델 파일 반입 필요) vs B안(AI 없이 키워드 검색으로 단순화, 품질
    하락하나 훨씬 간단) 중 아직 미정 — 사용자 결정 대기 중.
- **자기평가 기반 4대 개선 — 4개 전부 완료·배포됨(챗봇 회귀 버그 원인 규명·수정
  포함)**: Claude가 스스로 진단(자동테스트 부재/수동백업만 있음/긴급 알림 없음/챗봇
  느림)해서 제안 → 사용자 승인 → 진행.
  1. **핵심 기능 자동 회귀 테스트 — 완료**: `tests/e2e/`에 Playwright 4종(게이트+로그인,
     빠른저장, 위젯드래그 순서유지, 챗봇 정상응답) 추가, 실행 명령어 README 기록.
  2. **자동 백업 — 완료**: 최초엔 매일 1회였으나 사용자 요청으로 **주 1회(매주 일요일
     새벽 3:10 KST)**로 재조정 완료, "자동백업 YYYY-MM-DD" 라벨, 최근 **12개**(약
     3개월치) 유지 후 자동 삭제(수동 백업본은 대상 제외). pg_cron 등록 확인됨, 다음
     실행은 9/21(일).
  3. **대시보드 "신규 긴급 등급" 인앱 배너 — 완료**: 마지막 확인 이후 새로 긴급 등급된
     전동기 상단 배너 표시(2대 표시→확인 후 사라짐 확인), 시리얼 클릭 시 이동.
  4. **챗봇 응답 속도 개선 — 완료, 회귀 버그도 원인 규명 후 수정·배포 완료
     (commit `b6055adae6734eab067736c05532ff699161ca42`)**: 하이브리드 검색을
     `Promise.all`로 병렬화해 응답 속도 개선 + 단계별 진행 표시 추가 후, 배포 직후
     챗봇 문서 질의응답이 전부 "문서 검색 중 오류가 발생했어요"만 반복하는 회귀가
     발생했었음. **처음엔 병렬화 로직 버그로 추정했으나, Lovable이 서버 함수를 직접
     호출해 재현한 결과 실제 원인은 병렬화와 무관한 AI 게이트웨이의 별도 일일 사용
     크레딧 소진(402 에러, 당시 잔여 2.3/5)**이었음 — 이게 기존 코드에서 애매한
     "검색 중 오류"로만 표시되던 것. 근본 수정: `AiUnavailableError` 타입 도입해
     크레딧소진/차단/혼잡 등 AI 사용불가 상황을 "찾지 못함"과 명확히 구분(상태코드별
     정직한 안내 문구), 병렬 검색 중 일부만 실패해도 나머지 결과로 정상 응답(전부
     실패할 때만 오류 처리). 크레딧 재충전 후 브라우저로 직접 검증: 질문 1개 테스트
     결과 "AI 사용 크레딧이 모두 소진되어 문서 검색을 할 수 없습니다. 관리자에게
     크레딧 충전을 요청해 주세요."라는 정직한 문구가 정확히 표시됨(예전의 애매한
     오류 문구 아님) — 스크린샷 확인, 타입체크 통과 후 배포 완료.
     - **정상 응답 전체 검증 — 아직 미완료, 원인은 별도의 AI 사용 크레딧 소진**:
       "브러시 규격?", "정기검사 주기", "정류자 삭정 기준이 뭐야?", "브러시·정류자
       정비 방법" 4개 질문 모두 실제로 재테스트했으나 4건 전부 "AI 사용 불가" 안내만
       뜸(정직한 에러 문구 자체는 정상 동작 재확인됨). **핵심 발견**: Lovable
       워크스페이스 크레딧(메시지 발송용)과 AI 게이트웨이 사용 크레딧(챗봇 임베딩·
       생성 호출용)은 서로 다른 별도의 과금 항목 — 지금까지 충전한 것은 전자였고,
       후자(월간 기본 100 + 이월 100 + 보너스 15)가 완전히 소진되어 일일 잔여분
       (4.4)만으로는 AI 호출 자체가 거부됨. **다음 단계**: Lovable 결제 설정에서
       "AI 사용량" 관련 크레딧을 별도로 충전해야 정상 답변 검증이 가능함 — 사용자
       확인 대기 중.

- **담당자 비밀번호 확인 모달(useStaffGate) 렌더링 버그 — 완료, 배포됨
  (commit `a9161633c6b5ee8954bbcd621f6d6cb9897f892d`)**: 빠른저장 등에서 뜨는 비밀번호
  확인창이 화면 우측 상단쯤에 붕 떠서 나오고 스크롤하면 화면 밖으로 사라지던 버그를
  `@radix-ui/react-dialog` 기반 포탈 모달로 재구현해 해결. 어느 화면에서 호출해도
  뷰포트 기준 중앙 고정, 전체화면 딤+블러 배경, 스크롤해도 위치 유지, 배경 클릭/Esc로
  닫기 모두 정상 동작 확인(취부현황판·데이터관리·원본데이터 3개 화면 브라우저 직접
  검증). 덤으로 취부현황판 표 오른쪽의 원인 불명 빈 회색 박스도 제거(칸 미선택 시
  불필요한 빈 상세영역이 표시되던 레이아웃 버그였음 — 자산 선택 시에만 상세 패널
  표시하도록 수정). 회귀 방지용 Playwright 테스트(`tests/e2e/staff-gate-modal.spec.ts`)
  추가, 타입체크 통과.

## 파일 설명
- `raw_original.json` — 사용자가 제공한 원본 데이터 (master 171건 + repl 128건, 변환 전).
- `convert_fixed.py` — `raw_original.json` → 앱 스키마로 변환하는 스크립트 (CAR_PREFIX 공식 포함).
- `fixed_master.json` / `fixed_repl.json` — 변환된 결과 (DB와 동일, 128건 전체 포함).
- `repair_history_migration.sql` — 실제 DB에 삽입 완료된 SQL (RP-0082 제외 127건분). 재실행 불필요.
- `사내폐쇄망_이전_계획.md` — 사내망 이전 4단계 계획.
- `backup_20260814/` — 워크스페이스 이전 작업 전 백업한 tm_assets/repair_history/external_repair_log 스냅샷.
