# TM-CBM 원본 데이터 마이그레이션 — 진행 상황 (2026-08-12 기준, 최신)

Lovable 프로젝트(tm-cbm, project_id `b386a24f-2d99-4a20-b824-3efe76ecfa76`,
workspace `GS8VAndim2d3OBu4TcTX` = "권휘진's Lovable", free 플랜)에
원본 데이터를 반영하는 작업 기록.

## 완료된 것
- `tm_assets` 171건, `repair_history` **128건(원본 전체)** 전부 DB에 삽입 완료.
  111~116편성 각 24대 + 예비품 27건. 위치코드(M01~M24)는
  `CAR_PREFIX=[11,12,14,15,17,18]` + 편성 끝 2자리 공식으로 정확한 실차호(예: `1711`)로 변환됨.
- 예비품 `9134547`: 제작년도/제조사 **1991년/도시바**로 확정, DB 반영 완료.
- **RP-0082 반영 완료** — 113편성 1413-TM3, 취거88TWDH114→부착88091-73, 날짜 **2023-07-03** 확정.
- **src/routes/index.tsx 차호 표시 수정 완료** — `{car}차` → `{car}호차` (커밋 `b5c9b50`).
- **전체 기능 QA 1차 완료** — 챗봇/고장추세배지/대시보드 테스트, 버그 2건 발견·수정
  (편성 오인식, "3호차" 순번 질의 매칭 오류). 고장추세 배지·대시보드는 버그 없음 확인.
- **챗봇 인사말 버그 수정 완료** — "안녕" 등에 자연스러운 인사+안내로 응답.
- **시리얼 `???`(111편성 1511-TM4)** — 사용자 확인: 원본 자체가 미상이라 그대로 둠. 조치 불필요.
- **외부수선이력 완료** — `external_repair_log` 테이블 신설(47건: 수선반출 35 + 불용 12),
  `/external-repair` 페이지 추가(검색/필터/예비품 연결배지). 브라우저 테스트 검증함.
- **구글시트 연동 검토 후 취소** — Google Sheets/Drive는 "App User 커넥터" 방식이라
  워크스페이스 소유자가 Google Cloud Console에서 OAuth 웹 클라이언트를 직접 발급받아
  등록해야 함(일반 사용자가 클릭 몇 번으로 끝낼 수 없는 개발자 작업). 사용자가 "너무
  복잡하면 쓰기 힘들다"고 판단해 취소, 아래 대체 기능으로 전환.

## 내일 할 일 — 딱 하나 남음

### "엑셀로 내보내기" 버튼 (크레딧 있으면 바로, 요청 문구 준비됨)
취부 현황판에 버튼 추가 → 클릭 시 tm_assets/repair_history/external_repair_log를
각 시트로 나눈 .xlsx를 즉시 생성해 다운로드. 로그인 불필요, 별도 설정 불필요
(구글 연동과 달리 지금 바로 만들 수 있음). 어제 크레딧 소진으로 전송만 실패했음.

```
구글시트 연동은 설정이 너무 복잡해서 취소할게. 대신 훨씬 간단한 기능을 만들어줘:

**"엑셀로 내보내기" 버튼 추가**
- 취부 현황판(메인 화면) 상단에 "📥 엑셀로 내보내기" 버튼 추가
- 클릭하면 현재 시점의 tm_assets(마스터), repair_history(수선이력), external_repair_log(외부수선이력) 데이터를 각각 시트로 나눈 .xlsx 파일로 생성해서 바로 다운로드
- 파일명은 "TM현황_YYYYMMDD.xlsx" 형식으로
- 컬럼명은 한글로 (시리얼, 편성, 위치, 상태, 제작년도, 제조사, 비고, 교환일자 등 — DB 컬럼과 매칭되게)
- 별도 로그인 없이 조회자도 다운로드 가능하게

브라우저로 직접 버튼 눌러서 실제 파일이 다운로드되는지, 데이터가 맞는지 확인해줘.
```

## 아직 미착수 (급하지 않음, 나중에)
- 단일 HTML 파일의 로컬 파일연동 기능(`btnXlsxLink`)을 참고 자료로 남겨둠.
- 사내 폐쇄망 이전 (GitHub export + 백엔드를 사내 PostgreSQL로 교체).

## 파일 설명
- `raw_original.json` — 사용자가 제공한 원본 데이터 (master 171건 + repl 128건, 변환 전).
- `convert_fixed.py` — `raw_original.json` → 앱 스키마로 변환하는 스크립트 (CAR_PREFIX 공식 포함).
- `fixed_master.json` / `fixed_repl.json` — 변환된 결과 (DB와 동일, 128건 전체 포함).
- `repair_history_migration.sql` — 실제 DB에 삽입 완료된 SQL (RP-0082 제외 127건분). 재실행 불필요.
